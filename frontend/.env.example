# Copy this to frontend/.env for local (non-Docker) development with `npm run dev`.
# Vite only exposes vars prefixed VITE_ to client code, and reads this file at
# dev-server start time.
VITE_API_URL=http://localhost:8000
